version: '3.8'
services:
  cyberwar:
    build: .
    ports:
      - "5000:5000"
      - "8080:8080"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./config:/app/config
    restart: unless-stopped
